framework:
    router:
        default_uri: '%env(DEFAULT_URI)%'

when@test:
    framework:
        router:
            default_uri: 'http://localhost'

when@prod:
    framework:
        router:
            strict_requirements: null
